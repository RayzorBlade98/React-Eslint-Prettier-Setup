# Basic React Eslint Prettier setup

## Packages to install

`npm install -D eslint eslint-config-prettier eslint-plugin-react-hooks prettier typescript-eslint`

## Command

`"lint": "tsc --noEmit && eslint . --fix && prettier --write ."`
